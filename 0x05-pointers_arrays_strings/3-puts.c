#include "main.h"

/**
 * _puts - prints a string, followed by a new line, to stdout.
 *
 * @s: input.
 *
 **/

void _puts(char *str)
{
	int i = 0;

	while (s[i] != '\0')
	{
		putchar(s[i]);	
		i++;
	}
		putchar('\n');
}
